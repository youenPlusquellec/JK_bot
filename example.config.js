module.exports = {
	embedColor: '#e73b6d',		// Color of embed messages
	deployOnStart: false, 		// deploy commands on bot start, set to false in dev mode
	dumpCronTab: "0 0 1 * * *", // save a dump every day at 1am
};