module.exports = {
	// Color of embed messages
	embedColor: '#e73b6d',

	// deploy commands on bot start, set to false in dev mode
	deployOnStart: false,

	// save a dump every day at 1am
	dumpCronTab: '0 0 1 * * *',
};